<h1>Supercar Index Bitches!</h1>

<h4><%= link_to "Upload Supercar", "/supercars/new" %></h4>

<% @supercars.each do |supercar| %>
	<div>
	<a href="#{<%= supercar.photo_url%>}"></a> <%= link_to image_tag(supercar.photo_url), "/supercars/#{supercar.id}" %></p>
	</div>
<% end %>